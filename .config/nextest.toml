# cargo-nextest configuration.
#
# The coverage CI job runs the suite via `cargo llvm-cov nextest`, which
# executes each test in its OWN PROCESS. Several DB integration tests serialize
# themselves with an *in-process* `tokio::Mutex` because they mutate GLOBAL,
# cluster-wide state and assert on exact results:
#
#   * `scan_result_service.rs` `CLEANUP_TEST_LOCK` — the `cleanup_stuck_scans_*`
#     tests share the global stuck-scan reap query + `pg_try_advisory_xact_lock(
#     STUCK_SCAN_LOCK_ID)`. Concurrent ticks see the lock held by a sibling and
#     reap 0, failing `assert!(reaped >= 1)`.
#   * `storage_gc_service.rs` `STORAGE_GC_TEST_LOCK` (via `storage_gc_test_guard`)
#     — the `test_run_gc_*` tests run a cluster-wide GC reap and assert on the
#     exact set of keys collected. A concurrent GC test reaps another's fixtures
#     first, failing e.g. "GC result should include the upload temp and part keys".
#     The other two `storage_gc_test_guard()` callers join them for the same
#     reason: `api::handlers::storage_gc::tests` (#3501) drives a LIVE pass
#     through the admin route, and `oci_v2.rs`
#     `completion_digest_mismatch_delete_failure_is_recoverable_by_storage_gc`
#     needs its leaked temp object to survive until its own GC call reclaims
#     it — observed failing as "storage GC must delete leaked final PUT temp
#     object" when another test's live pass reaped it first.
#   * `sync_worker.rs` `recover_expired_sync_task_claims` — the
#     `expired_sync_task_claims_recover_and_fence_stale_owner` and
#     `legacy_in_progress_sync_task_without_claim_metadata_recovers` tests seed an
#     expired / legacy `in_progress` `sync_tasks` row, then call the recovery
#     sweep, which UPDATEs *every* stale claim in the DB (no peer scope) and assert
#     on the exact global count (`recovered == 1`). A sibling test's stale row is
#     swept in the same pass, failing the count/fence assertions. (The peer-scoped
#     `claim_pending_sync_tasks` concurrency tests only touch their own unique
#     peer's rows via `WHERE peer_instance_id = $1` and hold live-TTL claims the
#     sweep cannot match, so they stay parallel.)
#   * `repositories.rs`
#     `rest_delete_index_unwind_rolls_back_when_soft_delete_fails_db` (#3476) —
#     proves the REST delete's OCI index unwind and the `artifacts` soft-delete
#     are ONE transaction by injecting a `BEFORE UPDATE` trigger on `artifacts`.
#     The trigger's `WHEN (NEW.id = '<uuid>')` clause makes it inert for every
#     other row, but it is attached to the SHARED `artifacts` table for the
#     duration of the test, so no other `artifacts`-mutating test may be
#     mid-UPDATE while it exists.
#   * `oci_migration_reindex.rs` `run_repair` (#2457) — the `test_run_repair_*`
#     tests exercise the startup reindex, whose candidate scan, hollow-tag flag,
#     and orphan-tag DELETE are all CLUSTER-WIDE (no repository scope) and which
#     the tests assert on via the returned `RepairStats` (`manifests_registered`,
#     `orphan_tags_reconciled`). Run process-per-test against the shared DB, a
#     sibling `run_repair` call registers or drops another test's seeded
#     manifest / orphan tag first, so the count is attributed to the wrong test
#     (e.g. `orphan_tags_reconciled == 0`). Serializing them restores the
#     exact-count assertions the single-process `cargo test` run provided.
#   * `storage_stats_service.rs` `db_tests`, the `scheduler_service.rs`
#     `storage_gc_tick_follow_on` tests, and `repositories.rs`
#     `storage_tree_*_db` (#4004) — these run
#     `StorageStatsService::recompute_all` / `recompute_path_stats`, which
#     rewrite the WHOLE `repository_path_storage_stats` table, take a lock on
#     every `repositories` row, and stamp the CLUSTER-WIDE
#     `instance_storage_stats` singleton and `scheduler_leases` rows they then
#     assert on. `tdh::path_stats_serial_lock()` is a Postgres *session*
#     advisory lock and so does serialize these tests against each OTHER
#     across nextest processes — but not against the suites that delete
#     repositories without taking it, above all `storage_gc_service::tests`,
#     whose fixtures are repositories deleted mid-recompute. That ordering is
#     what deadlocked (#4004) and what left `computed_at` un-advanced while
#     `run_scheduled_refresh` still reported success. The product fix removes
#     the deadlock and retries the loser, so this group is about the residue
#     the lock order cannot reach: a repository legitimately vanishing between
#     a recompute's snapshot and its write. Putting them in the SAME group as
#     `storage_gc_service::tests` is the point — the group, not the filter
#     entry, is what orders them against the deleters.
#   * `middleware/setup.rs` `tests` (#3723) — the setup-gate tests seed
#     `users` rows that are `is_admin AND must_change_password` and assert on
#     the CLUSTER-WIDE `setup_still_required` predicate (no username scope):
#     one test asserts it is *false* while an inactive admin is pending, which
#     a sibling's active pending admin, alive at the same moment, would flip.
#     The bin-side `provision_admin_user_does_not_arm_gate_for_inactive_admin`
#     test seeds exactly such a row (its reactivated control phase), so it
#     joins the group too — the group spans test binaries within one run.
#   * `ci_rekey_` tests in `ci_oidc_service.rs` and `api/handlers/ci_auth.rs`
#     — the migration-232 tests re-run the CI service-account re-key, which
#     rewrites EVERY legacy-shaped `auth_provider = 'ci'` row it can attribute
#     to one identity mapping, cluster-wide. The adoption tests seed exactly
#     such rows and assert they are still legacy-keyed when the exchange runs
#     (or, for the ambiguity cases, afterwards); a concurrent migration run
#     re-keys them first and the assertions see the wrong principal.
#
# Under nextest's process-per-test model those in-process mutexes do NOT
# serialize across the suite, so the tests interfere nondeterministically, fail,
# and abort the instrumented run before `lcov.info` is written — leaving the
# Code Coverage gate red (#1753).
#
# There is no longer a single-process unit-test job to fall back on (#3479).
# The CI unit-test job — which is also the coverage run, since the separate
# coverage job was folded into it — runs
# `cargo nextest run --workspace --lib --bins --test-threads 8`, so the groups
# below are the ONLY thing serializing these suites in CI. They are not a
# coverage-specific workaround, and removing an entry breaks the unit-test gate
# outright rather than degrading one job.
#
# CI now runs that suite as a matrix of test shards (scripts/ci/test-shards.py),
# one leg per shard, each with its own Postgres. A group therefore only has
# to serialize the members that land in the same shard -- members in
# different legs cannot see each other's rows -- but a local run of the whole
# suite still has every member in one invocation, so the filters below stay
# complete and shard-agnostic.
#
# An earlier revision of this comment claimed a plain single-process
# `cargo test` job kept the in-process mutexes working there. That job no
# longer exists, and the premise did not generalize anyway: the `sync_worker`
# entries below have no in-process mutex at all — under a multi-threaded
# single-process run they fail today — so for those the groups are the only
# serialization that has ever existed.
#
# Pin every test that mutates this shared DB state to a single-threaded test
# group so nextest never runs two of them at once. Add new tests that take
# STUCK_SCAN_LOCK_ID or `storage_gc_test_guard()`, that call
# `recover_expired_sync_task_claims` or `run_repair` and assert on its global
# result, or that call `StorageStatsService::recompute_all` /
# `recompute_path_stats` directly and assert on what it wrote, to the matching
# filter below.

[test-groups]
db-serial = { max-threads = 1 }

[[profile.default.overrides]]
filter = 'test(cleanup_stuck_scans) | test(storage_gc_service::tests) | test(api::handlers::storage_gc::tests) | test(completion_digest_mismatch_delete_failure_is_recoverable_by_storage_gc) | test(sync_worker::tests::expired_sync_task_claims_recover_and_fence_stale_owner) | test(sync_worker::tests::legacy_in_progress_sync_task_without_claim_metadata_recovers) | test(oci_migration_reindex::tests::test_run_repair) | test(rest_delete_index_unwind_rolls_back_when_soft_delete_fails_db) | test(middleware::setup::tests) | test(provision_admin_user_does_not_arm_gate_for_inactive_admin) | test(storage_stats_service::db_tests) | test(scheduler_service::tests::storage_gc_tick_follow_on) | test(scheduler_service::tests::test_storage_gc_tick_follow_on) | test(/^api::handlers::repositories::tests::storage_tree_.*_db$/) | test(/::ci_rekey_/)'
test-group = 'db-serial'

# The integration targets under `backend/tests/` (nextest kind `test`) ALL
# run one at a time. Their DB-backed cases share schema state through global
# DELETEs in their cleanup helpers and seed rows that cluster-wide queries
# assert on, so two of them interleaving corrupts each other's fixtures. That
# serialization used to be `cargo test ... -- --ignored --test-threads=1` in
# a job of its own; since the integration suite runs inside the unit-test job
# (reusing its build), it is expressed here instead, where it holds for any
# invocation, whatever `--test-threads` says. It is the same `db-serial` group
# as the lib entries above on purpose: those mutate the same global state, so
# if the two sets ever share an invocation they must not overlap either.
# The non-#[ignore]d Tier 1 integration targets are serialized too; they are
# ~50 short tests and several of them open the same database.
[[profile.default.overrides]]
filter = 'kind(test)'
test-group = 'db-serial'
